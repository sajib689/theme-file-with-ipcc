<h1>{{ blog.title }}</h1>

{% for article in blog.articles %}
  <div>
    <h2>
      <a href="{{article.url}}">
        {{ article.title }}
      </h2>
      <h6>{{ article.author }}</h6>
      <div>{{ article.article-excerpt_or_content }}</div>
    </a>
  </div>
{% endfor %}